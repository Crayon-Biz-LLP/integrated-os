name: Graph Backfill (DISABLED)

# DISABLED: Sep 1 2026 — backfill_graph.py retired.
# Historical entity linking is now handled by:
#   1. Suggestion card pipeline (new messages)
#   2. _backfill_existing_content_for_entity (on node approval)
#   3. One-time script (scripts/archive/one_time_backfill.py) if needed
#
# To re-enable: restore the schedule block and workflow_dispatch below.

on: []
  # schedule:
  #   - cron: '0 20 * * *'
  # workflow_dispatch:

jobs:
  backfill:
    runs-on: ubuntu-latest
    if: false  # Permanently disabled

    steps:
      - name: Disabled
        run: echo "This workflow is disabled. See comments above."
